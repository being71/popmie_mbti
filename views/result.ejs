<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hasil MBTI Kamu</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body class="result-page">
    <div class="container py-5">
      <div class="text-center mb-5 text-white">
        <h1 class="fw-bold text-dark">✨ Hasil MBTI Kamu ✨</h1>
        <p class="lead text-dark">
          Inilah hasil yang paling menggambarkan kepribadianmu dalam bentuk Pop
          Mie!
        </p>
      </div>

      <!-- Hasil Card -->
      <div
        class="card shadow-lg border-0 mx-auto result-card bg-white bg-opacity-90"
      >
        <div class="card-body p-4 text-center">
          <div class="row align-items-center mb-4">
            <div class="col-md-6 mb-3 mb-md-0">
              <img
                src="/images/mbti/<%= result.type %>.png"
                alt="<%= result.type %>"
                class="img-fluid rounded shadow result-img"
              />
            </div>
            <div class="col-md-6">
              <img
                src="/images/popmie/<%= result.popmieImage %>"
                alt="<%= result.popmieName %>"
                class="img-fluid rounded shadow result-img"
              />
            </div>
          </div>

          <h3 class="fw-bold mb-3 result-title text-dark">
            <%= result.type %>
          </h3>
          <h3 class="fw-bold mb-3 result-popmie popmie-name text-dark">
            <%= result.popmieName %>
          </h3>
          <p class="fs-5 result-desc text-justify text-dark">
            <%= result.description %>
          </p>

          <div class="mt-4">
            <a href="/" class="btn btn-primary btn-lg">Coba Lagi</a>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
